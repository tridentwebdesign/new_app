<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Allray" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <nav class="main-nav">
        <div class="nav-container">
          <div>
            <%= link_to "Allray ホーム", root_path, class: "nav-link bold" %>
            <%= link_to "投稿一覧", posts_path, class: "nav-link" %>
            <%= link_to "新規投稿", new_post_path, class: "nav-link" %>
          </div>
          <div>
            ようこそ、<%= current_user.email %>さん |
            <%= link_to "ログアウト", session_path, data: { turbo_method: :delete }, class: "nav-link" %>
          </div>
        </div>
      </nav>
    <% else %>
      <nav class="main-nav">
        <div class="nav-container">
          <div>
            <%= link_to "Allray", root_path, class: "nav-link bold" %>
          </div>
          <div>
            <%= link_to "ログイン", new_session_path, class: "nav-link" %>
            <%= link_to "サインアップ", new_user_path, class: "nav-link" %>
          </div>
        </div>
      </nav>
    <% end %>

    <% if notice %>
      <div class="notice">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
